# --- Bootstrap deps (data download/load) ---
datasets>=2.19
huggingface_hub>=0.23
pyarrow>=16.0
pandas>=2.2
tqdm>=4.66
pyyaml>=6.0
# polars>=1.0          # optional faster parquet loading

# --- Added later in the "Set up tooling" task (Phase 0) ---
# torch>=2.3
# mlflow>=2.13
# faiss-cpu>=1.8
# fastapi>=0.111
# uvicorn>=0.30
# xgboost>=2.0
# scikit-learn>=1.5
