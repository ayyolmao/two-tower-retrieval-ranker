# --- Bootstrap deps (data download/load) ---
datasets>=2.19
huggingface_hub>=0.23
pyarrow>=16.0
pandas>=2.2
tqdm>=4.66
pyyaml>=6.0
polars>=1.0

# --- ML / serving deps (Phase 0 tooling setup) ---
torch>=2.5             # Python 3.13 support added in 2.5; MPS backend on Apple Silicon
mlflow>=2.14
faiss-cpu>=1.8         # arm64 wheel available from 1.8
fastapi>=0.111
uvicorn[standard]>=0.30
xgboost>=2.1           # 2.1+ has Python 3.13 wheels
scikit-learn>=1.5
